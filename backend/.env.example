# Database Configuration
DATABASE_URL=postgresql://postgres:xecwi9-nubtip-badmIb@db.acwvdxbgqkmrtrhsvhik.supabase.co:5432/postgres

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here

# Embedding Model Configuration
BGE_MODEL_PATH=./app/ml_models/bge-m3
BGE_MODEL_NAME=BAAI/bge-m3

# Rate Limiting (centralized configuration)
# Natural Mode (LLM-powered chatbot)
RATE_LIMIT_NATURAL_DAILY=20      # Max requests per day per IP
RATE_LIMIT_NATURAL_MONTHLY=100   # Max requests per month per IP

# Listen Mode (search-only, no LLM)
RATE_LIMIT_LISTEN_DAILY=40       # Max requests per day per IP
RATE_LIMIT_LISTEN_MONTHLY=200    # Max requests per month per IP

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Application Settings
ENVIRONMENT=development
DEBUG=True
LOG_LEVEL=INFO

# Server Configuration
HOST=0.0.0.0
PORT=8000
