[phases.setup]
# Python wird automatisch erkannt

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python scripts/download_model.py"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"